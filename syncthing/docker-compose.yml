syncthing:
  image: syncthing
  ports:
   - 8384:8384
   - 22000:22000
   - 21027:21027/udp
  volumes:
    - ./data:/syncthing
    - /etc/localtime:/etc/localtime:ro
  environment:
    - GID=1000
    - UID=1000
  restart: unless-stopped

syncthing-inotify:
  image: syncthing-inotify
  links:
    - syncthing:syncthing
  volumes:
    - /etc/localtime:/etc/localtime:ro
  volumes_from:
    - syncthing
  environment:
    - GID=1000
    - UID=1000
  restart: unless-stopped